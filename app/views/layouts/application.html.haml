!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title= title_tag
    %meta{:content => "width=device-width,initial-scale=1", :name => "viewport"}/
    %meta{:content => "yes", :name => "apple-mobile-web-app-capable"}/
    %meta{:content => "SessionHero", :name => "application-name"}/
    %meta{:content => "yes", :name => "mobile-web-app-capable"}/
    = csrf_meta_tags
    = csp_meta_tag
    = yield :head
    = render "shared/favicons"
    = render "shared/analytics"
    %link{:href => "https://fonts.googleapis.com", :rel => "preconnect"}/
    %link{:crossorigin => "", :href => "https://fonts.gstatic.com", :rel => "preconnect"}/
    %link{:href => "https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600;700&display=swap", :rel => "stylesheet", "data-clarity-unmask" => "true"}/
    = stylesheet_link_tag :app, "data-turbo-track": "reload", "data-clarity-unmask": "true"
    = javascript_importmap_tags
  -# Navbar is sticky (in normal flow), so the demo banner can sit above it at
  -# the very top; no fixed-navbar offset needed.
  %body.min-h-screen.flex.flex-col
    = render "shared/demo_banner"
    = render 'shared/navbar'
    = render 'shared/flash'
    = render "immosquare-cookies/consent_banner"
    %main.container.mx-auto.mt-12.px-5.flex.flex-1.w-full.items-start
      = yield
    %footer.border-t.border-base-300.mt-12
      .container.mx-auto.px-5.py-6.text-center.text-sm.opacity-70
        = render "shared/made_with"
